import React from 'react';
import { render } from 'react-dom';
import App from './components/app';

render(<App/>, document.querySelector('#groupParentInformation'));
// render(<App />, app);  This is how it looked in Kelly's
